{
  "name": "logo-cut-out",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Client-side background remover that never sends your images anywhere and never recompresses the pixels it keeps.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "generate:assets": "node scripts/generate-assets.mjs",
    "verify:cutout": "node scripts/verify-cutout.mjs"
  },
  "dependencies": {
    "@imgly/background-removal": "^1.5.9",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "sharp": "^0.35.4",
    "vite": "^6.0.7"
  }
}
